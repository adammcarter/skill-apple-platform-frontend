# Example Implementation Recipes

Use these only when the task explicitly needs a concrete example shape. Load the smallest matching recipe rather than the whole set.

## Quick Picks

- Need a screen-state example with loading, empty, loaded, or failed handling? Start with `empty-states-and-actions.md`.
- Need toolbar, control, or compact state-driven UI behavior? Start with `controls-and-state.md`.
- Need hierarchy, spacing, typography, or restrained chrome polish? Start with `visual-polish.md`.
- Need preview coverage or snapshot-friendly examples? Start with `previews-and-snapshots.md`.
- Need a macOS sidebar, inspector, or desktop composition example? Start with `macos-layout.md`.
- Need a narrow AppKit bridge from SwiftUI? Start with `appkit-interop.md`.

- `./example-recipes/empty-states-and-actions.md`
  - empty states with clear actions
  - explicit loading, empty, loaded, and failed UI state
- `./example-recipes/controls-and-state.md`
  - toolbar actions with native labels and SF Symbol discipline
  - small state-driven visual transitions
- `./example-recipes/visual-polish.md`
  - hierarchy through spacing, type, and restrained chrome
  - one clear focal point without card-heavy noise
- `./example-recipes/previews-and-snapshots.md`
  - multi-state previews
  - snapshot-friendly state matrices
- `./example-recipes/macos-layout.md`
  - macOS sidebar and inspector structure
- `./example-recipes/appkit-interop.md`
  - lightweight AppKit interop from SwiftUI

If none of these match, do not auto-load examples just to "have more context."
